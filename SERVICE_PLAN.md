# OpsTriage 서비스 기획서

> 장애 증상과 로그를 입력하면 AI가 장애 상황을 분석하고 초동 대응 절차를 제안하는 DevOps 지원 서비스

- 문서 버전: 1.0
- 작성일: 2026-08-21
- 프로젝트 유형: AI 기반 DevOps 웹 서비스

## 1. 서비스 개요

### 1.1 서비스명

**OpsTriage**

### 1.2 한 줄 소개

장애 증상과 로그를 입력하면 AI가 가능한 원인, 우선 확인 항목, 완화 방안과 장애 공지 초안을 구조화하여 제공하는 서비스이다.

### 1.3 기획 배경

서비스 운영 중 장애가 발생하면 엔지니어는 로그, 최근 배포 내역, 시스템 상태 등을 빠르게 확인해야 한다. 하지만 경험이 부족한 주니어 엔지니어나 소규모 개발팀은 무엇부터 확인해야 하는지 판단하기 어렵다.

OpsTriage는 사용자가 입력한 장애 정보와 로그를 AI가 분석해 가능한 원인, 우선 확인 항목, 완화 방안과 장애 공지 초안을 구조화하여 제공한다.

### 1.4 서비스 목적

- 장애 발생 시 필요한 초동 대응 정보를 빠르게 정리한다.
- 주니어 DevOps 엔지니어의 장애 분석을 보조한다.
- 장애 대응 절차를 일관된 형식으로 제공한다.
- 장애 상황을 팀에 공유할 수 있는 공지 초안을 생성한다.

OpsTriage의 결과는 운영자의 판단을 돕는 참고 자료이며, 실제 시스템을 직접 변경하거나 명령어를 자동 실행하지 않는다.

## 2. 타겟 사용자

### 2.1 주요 사용자

- DevOps 또는 SRE 직무를 준비하는 학습자
- 장애 대응 경험이 부족한 주니어 DevOps 엔지니어
- 전담 인프라 운영자가 없는 소규모 개발팀
- 배포와 운영을 함께 담당하는 백엔드 개발자

### 2.2 사용자 문제

- 장애 발생 시 무엇부터 확인해야 할지 알기 어렵다.
- 긴 로그에서 중요한 내용을 빠르게 찾기 어렵다.
- 장애 심각도와 대응 우선순위를 결정하기 어렵다.
- 장애 현황을 팀에 전달할 문장을 작성하는 데 시간이 걸린다.
- 대응 과정을 Runbook 형태로 구조화하기 어렵다.

## 3. 핵심 가치

OpsTriage는 사용자의 장애 정보를 다음과 같은 구조로 변환한다.

```text
정리되지 않은 장애 정보
→ 장애 상황 요약
→ 심각도 제안
→ 가능한 원인
→ 우선 확인 항목
→ 완화 및 롤백 방안
→ 팀 공유용 공지 초안
```

사용자는 AI의 답변을 그대로 실행하는 것이 아니라, 제안된 내용을 바탕으로 실제 시스템 상태를 확인하고 대응 여부를 판단한다.

## 4. 페이지 및 섹션 구성

서비스는 하나의 웹 페이지 안에 4개 섹션으로 구성하며, 상단 메뉴를 통해 각 섹션으로 이동할 수 있도록 한다.

### 4.1 Home

- 서비스 이름과 핵심 소개
- 주요 기능 요약
- 서비스 이용 흐름
- "장애 분석 시작하기" 버튼

### 4.2 Incident Analyzer

AI 장애 분석 기능을 제공하는 핵심 섹션이다.

입력 항목:

- 서비스명: 선택, 최대 100자
- 운영 환경: 필수, Production, Staging, Development 중 선택
- 장애 증상: 필수, 최대 2,000자
- 로그 또는 오류 메시지: 선택, 최대 4,000자
- 최근 배포 및 설정 변경 사항: 선택, 최대 1,000자

장애 증상, 로그 또는 오류 메시지, 최근 배포 및 설정 변경 사항을 합한 분석 입력은 최대 4,000자로 제한한다.

제공 기능:

- AI 장애 분석 요청
- 분석 진행 상태 표시
- 분석 결과 출력
- 결과 복사
- 입력 및 결과 초기화

### 4.3 Response Guide

- 기본 장애 대응 순서
- 심각도 등급 안내
- 안전한 로그 입력 방법
- 민감정보 제거 안내
- AI 분석 결과 사용 시 주의사항

### 4.4 About & FAQ

- 프로젝트 제작 목적
- 기술 스택
- AI 분석의 한계
- 자주 묻는 질문
- GitHub 저장소 링크

## 5. 핵심 기능

### 5.1 장애 정보 입력

사용자는 서비스명, 운영 환경, 장애 증상, 로그와 최근 변경 사항을 입력한다. 운영 환경과 장애 증상은 필수 입력이며, 서비스명, 로그와 최근 변경 사항은 선택적으로 입력할 수 있다.

### 5.2 입력값 검증

API 호출 전에 브라우저에서 다음 사항을 확인한다.

- 장애 증상이 입력되었는가
- 공백만 입력되지 않았는가
- 서비스명이 100자를 초과하지 않았는가
- 장애 증상, 로그, 최근 변경 사항이 각 필드의 최대 길이를 초과하지 않았는가
- 분석 입력 세 필드의 합계가 4,000자를 초과하지 않았는가
- 요청이 중복으로 제출되지 않았는가

동일한 검증을 Python API에서도 다시 수행하여 클라이언트 검증 우회를 방지한다.

### 5.3 AI 장애 분석

AI는 입력된 정보를 바탕으로 다음 항목을 생성한다.

1. 장애 상황 요약
2. 예상 심각도
3. 가능한 원인과 판단 근거
4. 우선 확인 항목
5. 완화 또는 롤백 방안
6. 팀 공유용 장애 공지
7. 추가로 수집해야 할 정보

### 5.4 구조화된 결과 표시

분석 결과를 하나의 긴 문장이 아닌 카드 형태로 구분하여 표시한다.

예상 결과 구조:

```json
{
  "summary": "배포 이후 결제 API에서 502 오류가 증가한 상황",
  "severity": "SEV-2",
  "possible_causes": [
    {
      "cause": "업스트림 애플리케이션 응답 지연",
      "reason": "로그에 upstream timed out 메시지가 기록되어 있음"
    },
    {
      "cause": "애플리케이션과 데이터베이스 사이의 연결 문제",
      "reason": "배포 직후 응답 지연과 502 오류가 함께 증가함"
    }
  ],
  "first_checks": [
    "오류 발생 시점과 최근 배포 시점을 비교한다.",
    "애플리케이션 응답 시간과 오류율을 확인한다."
  ],
  "mitigations": [
    "문제가 최근 배포와 관련 있다면 이전 버전으로 롤백을 검토한다."
  ],
  "communication": "현재 결제 API 응답 지연 문제를 조사하고 있습니다.",
  "additional_information": [
    "최근 배포 버전",
    "응답 시간 변화",
    "데이터베이스 연결 상태"
  ]
}
```

### 5.5 결과 복사

분석 결과를 클립보드에 복사할 수 있도록 한다. 사용자는 결과를 장애 대응 문서나 팀 메시지 작성에 활용할 수 있다.

### 5.6 반응형 화면

데스크톱, 태블릿, 모바일 환경에서 입력 폼과 결과 카드가 정상적으로 표시되도록 구현한다.

## 6. AI 기능 설계

### 6.1 입력

- 서비스명: 선택, 최대 100자
- 운영 환경: 필수
- 장애 증상: 필수, 최대 2,000자
- 로그 또는 오류 메시지: 선택, 최대 4,000자
- 최근 배포 및 설정 변경 사항: 선택, 최대 1,000자

장애 증상, 로그 또는 오류 메시지, 최근 배포 및 설정 변경 사항의 합계는 최대 4,000자까지 허용한다.

### 6.2 출력

- 장애 요약
- 심각도 제안
- 원인 가설과 판단 근거
- 우선 확인 항목
- 완화 방안
- 공지 초안
- 추가 확인 정보

### 6.3 사용자에게 제공하는 가치

- 긴 장애 정보를 짧고 명확하게 요약한다.
- 초동 대응 순서를 구조화한다.
- 놓칠 수 있는 확인 항목을 제안한다.
- 장애 공유 문서를 작성하는 시간을 줄인다.

### 6.4 AI 응답 원칙

- 확인되지 않은 원인을 확정적으로 표현하지 않는다.
- 원인은 반드시 "가능성이 있는 가설"로 안내한다.
- 데이터 삭제나 시스템 중단처럼 위험한 명령을 직접 지시하지 않는다.
- 실제 작업 전에 담당자가 검토해야 한다는 안내를 표시한다.
- 입력에 없는 시스템 상태를 사실처럼 생성하지 않는다.

### 6.5 심각도 기준

AI가 제안하는 심각도는 아래 네 단계 중 하나로 제한한다. 이는 최종 판정이 아니라 운영자의 판단을 돕는 제안값이다.

| 등급 | 기준 |
|---|---|
| SEV-1 | 핵심 서비스 전체 중단, 데이터 손실 또는 광범위한 보안 영향 가능성 |
| SEV-2 | 핵심 기능의 심각한 성능 저하 또는 다수 사용자의 부분 장애 |
| SEV-3 | 일부 기능의 제한적 장애이며 우회 방법이 존재하는 상황 |
| SEV-4 | 사용자 영향이 거의 없는 경미한 문제 또는 모니터링이 필요한 상황 |

## 7. 실패 및 예외 처리

| 상황 | 사용자 안내 |
|---|---|
| 필수값 누락 | "장애 증상을 입력해주세요." |
| 공백만 입력 | "분석할 수 있는 내용을 입력해주세요." |
| 개별 입력 길이 초과 | "각 입력 항목의 최대 글자 수를 확인해주세요." |
| 전체 분석 입력 길이 초과 | "장애 증상, 로그와 최근 변경 사항은 합계 4,000자 이하로 작성해주세요." |
| 분석 진행 중 | "장애 상황을 분석하고 있습니다." |
| 중복 요청 | 분석 버튼을 비활성화하여 중복 호출 방지 |
| API 인증 오류 | "서비스 설정을 확인할 수 없습니다." |
| API 호출 한도 초과 | "요청이 많습니다. 잠시 후 다시 시도해주세요." |
| 서버 오류 | "분석 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요." |
| 시간 초과 | "응답이 지연되고 있습니다. 다시 시도해주세요." |
| 잘못된 응답 형식 | "분석 결과를 처리하지 못했습니다." |

오류 메시지에는 API 키, 서버 경로, 내부 오류 내용 등 민감한 정보를 표시하지 않는다.

## 8. 사용자 이용 흐름

1. 사용자가 Home에서 서비스 설명을 확인한다.
2. "장애 분석 시작하기" 버튼을 누른다.
3. 운영 환경과 장애 정보를 입력하고, 필요한 경우 서비스명을 추가한다.
4. "AI로 분석하기" 버튼을 누른다.
5. 입력값 검증 후 Python API를 호출한다.
6. 분석 중에는 로딩 메시지를 표시한다.
7. AI 분석 결과를 카드 형태로 보여준다.
8. 사용자는 결과를 복사하거나 새로운 분석을 시작한다.

## 9. 기술 구성

### 9.1 프론트엔드

- HTML
- CSS
- Vanilla JavaScript
- Fetch API

### 9.2 백엔드

- Python
- Vercel Serverless Functions
- AI API
- JSON 기반 요청 및 응답

### 9.3 배포 및 형상 관리

- GitHub
- Vercel
- Git 커밋 이력 관리

### 9.4 환경 변수

```text
OPENAI_API_KEY
OPENAI_MODEL
```

`OPENAI_API_KEY`는 로컬 환경 파일과 Vercel 환경 변수로 관리하며 코드, README, GitHub 저장소 및 증빙 화면에 노출하지 않는다. `OPENAI_MODEL`은 선택 항목이며 기본값은 `gpt-5.4-mini`이다.

### 9.5 API 계약

프론트엔드는 아래 규격으로 Python Serverless Function을 호출한다.

```http
POST /api/analyze
Content-Type: application/json
```

요청 본문:

```json
{
  "service_name": "Payment API",
  "environment": "Production",
  "symptom": "배포 직후 결제 요청에서 502 오류가 증가했습니다.",
  "logs": "upstream timed out while reading response header from upstream",
  "recent_changes": "30분 전에 애플리케이션 새 버전을 배포했습니다."
}
```

성공 응답은 `200` 상태와 함께 아래 형식으로 반환한다. `data`의 내부 구조는 5.4의 구조화된 결과 형식을 따른다.

```json
{
  "success": true,
  "data": {
    "summary": "장애 상황 요약",
    "severity": "SEV-2",
    "possible_causes": [],
    "first_checks": [],
    "mitigations": [],
    "communication": "팀 공유용 공지 초안",
    "additional_information": []
  }
}
```

오류 응답은 사용자에게 표시할 수 있는 일반화된 메시지만 반환한다.

```json
{
  "success": false,
  "error": {
    "code": "RATE_LIMITED",
    "message": "요청이 많습니다. 잠시 후 다시 시도해주세요."
  }
}
```

- 잘못된 메서드는 `405`, 입력 오류는 `400`, 호출 한도 초과는 `429`, 외부 AI API 또는 서버 오류는 `500` 계열 상태로 처리한다.
- 프론트엔드는 `AbortController`를 사용해 30초 후 요청을 중단하고 시간 초과 메시지를 표시한다.
- Python API는 브라우저와 동일한 입력값 및 길이 검증을 수행한다.
- 내부 예외 메시지, API 키와 서버 경로는 응답에 포함하지 않는다.

## 10. 프로젝트 구조

```text
opstriage/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── app.js
├── api/
│   └── analyze.py
├── images/
├── evidence/
│   ├── desktop/
│   ├── mobile/
│   └── ai-tool/
├── README.md
├── SERVICE_PLAN.md
├── requirements.txt
└── .gitignore
```

## 11. 보안 및 운영 정책

- API 키는 Python 백엔드에서만 사용한다.
- 프론트엔드 코드에 API 키를 포함하지 않는다.
- 로그에 비밀번호, 토큰, 개인정보를 입력하지 않도록 안내한다.
- 입력된 장애 정보는 기본 버전에서 별도로 저장하지 않는다.
- 입력 길이를 제한해 불필요한 API 비용을 줄인다.
- 분석 중 버튼을 비활성화해 중복 요청을 방지한다.
- 사용자에게 내부 API 오류 상세 정보를 노출하지 않는다.
- AI 결과는 참고 정보이며 실제 운영 변경 전 검토가 필요함을 안내한다.

## 12. 테스트 계획

| 테스트 항목 | 입력 예시 | 기대 결과 |
|---|---|---|
| 정상 분석 | Nginx 502 오류와 타임아웃 로그 | 구조화된 분석 결과 표시 |
| 빈 입력 | 장애 증상 미입력 | 필수값 안내 |
| 개별 필드 길이 초과 | 장애 증상 또는 로그의 최대 길이 초과 | 해당 입력 길이 제한 안내 |
| 전체 입력 길이 초과 | 분석 입력 세 필드 합계 4,000자 초과 | 전체 입력 길이 제한 안내 |
| API 오류 | AI API 호출 실패 | 재시도 안내 |
| 응답 지연 | 설정 시간 이상 응답 없음 | 시간 초과 안내 |
| 중복 요청 | 버튼 연속 클릭 | 추가 요청 차단 |
| 응답 형식 오류 | 필수 출력 필드가 없는 AI 응답 | 결과 처리 오류 안내 |
| 모바일 화면 | 360px 화면 | 가로 스크롤 없이 정상 표시 |
| 데스크톱 화면 | 1440px 화면 | 입력 영역과 결과 카드 정상 표시 |
| 보안 확인 | GitHub 코드 검색 | API 키가 발견되지 않음 |

### 12.1 대표 시연 시나리오

```text
서비스명: Payment API
운영 환경: Production

장애 증상:
배포 직후 결제 요청에서 502 오류가 증가했습니다.

로그:
upstream timed out while reading response header from upstream

최근 변경 사항:
30분 전에 애플리케이션 새 버전을 배포했습니다.
```

기대 결과:

- 최근 배포와 오류 발생 시점의 연관성 안내
- 업스트림 응답 지연 가능성 제시
- 애플리케이션 로그와 응답 시간 확인 제안
- 필요 시 이전 버전 롤백 검토
- 팀 공유용 장애 공지 초안 생성

### 12.2 다크 모드 UX 개선 및 효과 측정

#### 개선 목표

- 야간 장애 대응 시 밝은 화면에서 느끼는 눈부심을 줄인다.
- 운영체제 설정을 자동으로 반영해 사용자가 별도 설정 없이 선호 테마를 사용할 수 있도록 한다.
- 사용자가 직접 선택한 테마를 다음 방문에도 유지해 반복 설정을 없앤다.
- 테마 전환으로 정보 구조, 심각도 색상과 입력 상태의 의미가 달라지지 않도록 한다.

#### 구현 기준

- 최초 방문 시 `prefers-color-scheme` 설정을 따른다.
- 상단 버튼으로 라이트 모드와 다크 모드를 전환한다.
- 사용자 선택은 브라우저 `localStorage`의 `opstriage-theme` 항목에만 저장한다.
- 페이지가 표시되기 전에 저장된 테마를 적용해 화면 깜빡임을 줄인다.
- 버튼은 키보드로 조작할 수 있으며 현재 상태와 다음 동작을 보조 기술에 전달한다.
- 애니메이션 감소 설정을 사용하는 경우 테마 전환 효과도 최소화한다.

#### 효과 확인 방법

5명 이상의 테스트 사용자가 동일한 장애 분석 과제를 라이트 모드와 다크 모드에서 수행하도록 하고 아래 지표를 비교한다.

| 측정 항목 | 측정 방법 | 목표 |
|---|---|---|
| 테마 전환 발견성 | 별도 설명 없이 테마 버튼을 찾고 전환하기까지 걸린 시간 | 참여자의 80% 이상이 10초 이내 전환 |
| 시각적 편안함 | 어두운 환경에서 분석 완료 후 5점 척도 설문 | 다크 모드 평균 4점 이상 |
| 과제 성공률 | 샘플 입력, 분석 요청, 결과 복사 완료 여부 | 두 테마 모두 100% 완료 |
| 가독성 유지 | 주요 텍스트와 입력 상태를 잘못 인지한 횟수 | 테마별 오인 0회 |
| 설정 지속성 | 새로고침 및 재방문 후 선택한 테마 유지 여부 | 모든 지원 브라우저에서 유지 |

테스트 결과는 참여자별 완료 시간, 성공 여부와 5점 만족도만 익명으로 기록하며 장애 입력 내용이나 개인정보는 수집하지 않는다.

## 13. 성공 기준

- 4개 섹션을 메뉴로 이동할 수 있다.
- 모바일과 데스크톱에서 화면이 깨지지 않는다.
- 사용자 입력이 Python API로 전달된다.
- AI 분석 결과가 화면에 구조화되어 표시된다.
- 빈 입력, API 오류, 시간 초과 상황을 안내한다.
- 배포된 Vercel URL에서 기능을 재현할 수 있다.
- GitHub에 의미 있는 단계별 커밋 이력이 존재한다.
- README에 실행, 배포, 환경 변수 설정 방법이 포함된다.
- API 키가 코드와 증빙 자료에 노출되지 않는다.

## 14. 개발 범위

### 14.1 필수 범위

- 반응형 웹 화면
- 4개 섹션과 메뉴 이동
- 장애 정보 입력 폼
- Python AI API 연동
- 구조화된 분석 결과
- 로딩 및 오류 처리
- Vercel 배포
- README와 증빙 자료

### 14.2 선택 범위

필수 기능을 완료한 경우 다음 기능을 추가한다.

- [x] 다크 모드
- [x] Markdown 형식 결과 복사
- [x] 샘플 장애 시나리오 자동 입력
- 최근 분석 내역의 브라우저 임시 저장
- 분석 결과 다운로드

## 15. 제출 산출물 체크리스트

- [ ] 배포된 웹 서비스의 Vercel URL
- [ ] 프로젝트 코드와 의미 있는 커밋 이력이 포함된 GitHub 저장소
- [ ] 서비스 소개, 기술 스택, 실행 및 배포 방법, 배포 URL, 환경 변수 설정법이 포함된 README
- [ ] 최종 내용이 반영된 `SERVICE_PLAN.md`
- [ ] 데스크톱 화면 스크린샷
- [ ] 모바일 화면 스크린샷
- [ ] AI 분석 기능이 정상 동작하는 장면의 스크린샷
- [ ] AI 코딩 도구의 대화 로그 또는 사용 과정 스크린샷
- [ ] 코드, README와 모든 증빙 자료에 API 키가 노출되지 않았는지 최종 확인

## 16. 기대 효과

OpsTriage를 통해 사용자는 장애 상황을 보다 빠르게 정리하고 초동 대응에 필요한 확인 항목을 얻을 수 있다. 또한 제작 과정에서 프론트엔드와 Python 백엔드의 연결, AI API 연동, 환경 변수 관리, 서버리스 배포와 장애 처리 흐름을 학습할 수 있다.

이 프로젝트는 DevOps 엔지니어에게 필요한 운영 안정성, 장애 대응, 자동화, 보안 및 문서화 관점을 함께 보여주는 것을 목표로 한다.
